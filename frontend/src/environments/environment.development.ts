export const environment = {
  production: false,
  // Use the Angular dev-server proxy (see proxy.conf.json) so external clients via ngrok
  // hit /api on the dev server and it forwards to the local backend.
  BASE_API: '/api',
};
