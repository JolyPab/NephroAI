name: nephroai_mobile
description: NephroAI — управление лабораторными анализами
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.3
  shared_preferences: ^2.3.2
  fl_chart: ^0.69.0
  file_picker: ^8.1.2
  provider: ^6.1.2
  go_router: ^14.2.7
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
